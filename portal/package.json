{
  "name": "isla-portal",
  "version": "1.1.0",
  "description": "Private HOA guest-paperwork and owner-review portal for Example Island Unit 405D",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "bash -lc 'for f in functions/[[path]].js functions/lib/*.js cron/src/*.js; do node --check \"$f\" || exit 1; done'",
    "build": "wrangler pages functions build --outdir .wrangler/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  }
}
