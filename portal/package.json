{
  "name": "isla-portal",
  "version": "1.1.0",
  "description": "Public repo (anonymized code identity) for the Unit 405D HOA guest-paperwork and owner-review portal; see README for identity and data-minimization policy.",
  "scripts": {
    "verify": "node scripts/verify.mjs",
    "test": "node --test test/*.test.js",
    "test:runtime": "node scripts/test_atomic_runtime.mjs && node scripts/test_restart_runtime.mjs",
    "check": "bash -lc 'for f in functions/[[path]].js functions/lib/*.js cron/src/*.js; do node --check \"$f\" || exit 1; done'",
    "build": "wrangler pages functions build --outdir .wrangler/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  }
}
